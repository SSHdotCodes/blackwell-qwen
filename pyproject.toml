[project]
name = "blackwell-qwen"
version = "0.1.0"
description = "Quality-gated tuning and serving for Qwen3.8-27B-FP8 on one RTX PRO 6000 Blackwell"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]

